def prefixPrintMsg(msg):
    """Prefix terminal message that will display to the user."""
    return f'Auto Downloader Twitch : {msg}'

def suffixException(msg):
    """Suffix sumSmash to the end of an exception message to reflect how the user must feel."""
    return f'{msg} sumSmash'
